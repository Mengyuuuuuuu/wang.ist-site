<%- include('layout', { title, body: `
<main class="content">
  <article class="note">
    <h1>${title}</h1>

    <div class="note-body">${content}</div>

    <p style="margin-top: 2rem">
      <a href="/notes">← Zurück zur Übersicht</a>
    </p>
  </article>
</main>
` }) %>
