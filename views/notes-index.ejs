<%- include('layout', { title: "Learning Notes – Mengyu Wang", body: `
<section>
  <h1>Learning Notes</h1>
  <ul>
    ${notes.map(note => `
    <li><a href="/notes/${note.slug}">${note.title}</a></li>
    `).join('')}
  </ul>
</section>
` }) %>
